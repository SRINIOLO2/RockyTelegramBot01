version: '3.8'

services:
  spotify-bot:
    image: deertraps/rocky-telegram-bot:latest
    container_name: spotify-telegram-bot
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
